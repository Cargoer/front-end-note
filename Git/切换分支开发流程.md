```shell
git fetch origin // if branch is recently created
git branch -r // see if branch exist
// backup your modified code
git checkout branch
git pull
// restore your modified code
git status // see if code modified
git add .
git commit -am '...'
git push
```

